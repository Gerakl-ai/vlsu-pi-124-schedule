/**
 * Версия выпуска.
 *
 * Раньше здесь стояла строка, которую надо было менять руками, — и её забывали.
 * Из-за этого адрес service worker не менялся между выпусками, а установленное
 * приложение могло месяцами жить на старой сборке. Для расписания это хуже
 * любой опечатки: студент смотрит данные, которых уже нет.
 *
 * Значение подставляется при сборке (см. vite.config.ts): в CI — из коммита,
 * локально — из времени сборки.
 */
declare const __RELEASE__: string;

export const RELEASE_CHANNEL = typeof __RELEASE__ === "string" ? __RELEASE__ : "dev";
