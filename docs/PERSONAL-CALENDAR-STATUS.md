# Личный календарь: реализация и ограничения

## Реализовано

- Личные события с названием, началом, окончанием, местом и описанием.
- Создание, редактирование и удаление с подтверждением из календаря.
- Отображение событий в выбранном дне и на экране «Сегодня» при доступном расписании.
- События, пересекающие полночь, входят в оба дня. В экспорте месяца событие не дублируется.
- Свайп месяца не перехватывает вертикальную прокрутку; случайный клик после свайпа подавляется.
- Убрана зависимость сброса календаря от изменяющегося callback закрытия.
- Быстрые светлый/тёмный режимы и цвет акцента. Палитры и точная настройка доступны отдельно.
- Переход вкладки использует короткую transform-анимацию без прежнего перекрывающего слоя.
- Ошибки сохранения записи и черновика больше не маскируются успешным сохранением.

## Проверено

`pnpm run check`: TypeScript, 197 тестов, production build.
`node --check public/sw.js`: успешно.
`scripts/qa-personal-calendar.cjs`: 402x874 и 874x402, свайп месяца,
создание, перезагрузка, изменение, удаление, сохранение режима темы,
без необработанных ошибок JavaScript. Скриншоты: `artifacts/qa/personal-calendar/`.
Это Chromium QA, не проверка на физическом iPhone.

## Ограничения

- События локальны для origin (`lad.personal-events.v1`), не синхронизируются между устройствами.
- JSON-резервная копия версии 7 содержит личные события; версии 1–6 по-прежнему принимаются.
  Конфликтующие события сохраняются отдельными вариантами, повторный импорт не размножает их.
  Импорт ICS пока не реализован. Несохранённые черновики не включены в JSON-копию.
- Отображение личных событий на Today при полностью отсутствующих учебных данных требует отдельного состояния.
- Перетаскивание, повторяющиеся события и фоновые уведомления не входят в эту реализацию.
- Производительность анимаций на физическом iPhone и клавиатура iOS требуют отдельного прогона.

## Следующий этап: данные и дизайн

1. Проверить PDF ИИТЭ осень 2026/2027: координаты таблиц, подгруппы, числитель/знаменатель,
   ограничения по неделям, период действия. Не превращать обычное извлечение текста в готовое расписание.
2. PDF охватывает один институт, не весь ВлГУ. Нужны проверенные источники остальных институтов.
3. Показывать покрытие реальными снимками, а не количеством групп в каталоге. Не обещать офлайн-доступ
   к расписанию, которое ещё никогда не получено или не загружено на устройство.
4. Ввести проверенный импорт со ссылкой на источник, датой и отчётом неоднозначностей.
5. Упростить Today до даты, текущей/следующей пары, списка и личных событий; убрать повторяющие
   эти данные декоративные сводки. Week сделать главным сравнительным представлением.
6. Проверить весь цикл на iPhone portrait/landscape, Android, reduced motion и отключённой сети.
